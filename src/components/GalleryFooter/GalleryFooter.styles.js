import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  imageViewFooter: {
    fontFamily: 'Poppins',
    fontSize: 16,
    color: 'white',
    textAlign: 'center',
    padding: 16,
  },
});
